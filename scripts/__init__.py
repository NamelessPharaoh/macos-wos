"""Standalone operator scripts (capability_report, burnin_report, refresh_knowledge).

Not a library: each module is meant to be run with `uv run python scripts/<name>.py`.
This file exists only so `scripts.refresh_knowledge` is importable from tests.
"""
